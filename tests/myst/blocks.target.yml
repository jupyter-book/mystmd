cases:
  - title: Header with id label
    mdast:
      type: root
      children:
        - type: heading
          depth: 1
          children:
            - type: text
              value: My Header
          identifier: my_id
          label: my_id
    myst: |-
      (my_id)=
      # My Header
    html: |-
      <h1 id="my_id">My Header</h1>
