# myst-roles

Default role definitions for MyST parser.

## Roles

These roles are ported from `markdown-it-docutils`:

- `sup`/`superscript`
- `sub`/`subscript`
- `abbr`/`abbreviation`
- `eq`
- `ref`
- `numref`
- `math`
