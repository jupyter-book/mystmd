export enum TemplateKind {
  tex = 'tex',
  typst = 'typst',
  docx = 'docx',
  site = 'site',
}

export enum TemplateOptionType {
  boolean = 'boolean',
  string = 'string',
  number = 'number',
  choice = 'choice',
  file = 'file',
}
