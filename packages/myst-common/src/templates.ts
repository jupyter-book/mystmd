export enum TemplateKind {
  tex = 'tex',
  docx = 'docx',
  site = 'site',
}

export enum TemplateOptionType {
  boolean = 'boolean',
  string = 'string',
  number = 'number',
  choice = 'choice',
  file = 'file',
}
